include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_single_quotes: true
    # The existing codebase intentionally uses compact single-statement flows.
    # Keep this style-only rule from blocking revenue release validation.
    curly_braces_in_flow_control_structures: false
